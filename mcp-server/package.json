{
  "name": "@task-board/mcp-server",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "MCP server for the task-board API (token auth)",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^4.0.2"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5"
  }
}
